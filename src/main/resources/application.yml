spring:
  application:
    name: cnwasshu
  profiles:
    default: local

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:8080,http://localhost:8081,http://localhost:19006}

kakao-mobility:
  rest-api-key: ${KAKAO_MOBILITY_REST_API_KEY:}
  base-url: ${KAKAO_MOBILITY_BASE_URL:https://apis-navi.kakaomobility.com}

weather:
  api:
    service-key: ${WEATHER_API_SERVICE_KEY}
    base-url: https://apis.data.go.kr/1360000/VilageFcstInfoService_2.0